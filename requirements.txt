# Core
python-dotenv>=1.0.0

# Markdown conversion
markitdown[all]>=0.1.0

# Language detection
langdetect>=1.0.9

# HTTP (Ollama API)
requests>=2.31.0

# Audio processing
pydub>=0.25.1
lameenc>=1.7.0

# Cloud LLM providers (Phase 3)
groq>=0.5.0
openai>=1.30.0

# Cloud TTS provider (Phase 3)
elevenlabs>=1.3.0

# Microsoft Edge TTS (free, no API key)
edge-tts>=6.1.0
# Pure-Python MP3 decoder used to convert Edge TTS output to WAV (no ffmpeg required)
miniaudio>=1.59

# Frontend
streamlit>=1.35.0

# Dev / testing (optional)
pytest>=8.0.0
